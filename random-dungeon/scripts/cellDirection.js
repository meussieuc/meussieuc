// -----------------------------------------------------------------------------// CellDirection Class// -----------------------------------------------------------------------------class CellDirection extends Vector {	// -----------	// constructor	// -----------   constructor(dirX, dirY, origin, border, grid) {      super(dirX, dirY);      		// ---------------		// Private members		// ---------------      this.origin = origin;      this.border = border;      this.grid   = grid;      		// ---------------		// Private methods		// ---------------         }		// ---------	// Accessors	// ---------   // Origin   // ------   get Origin() { return this.origin; }   // Destination   // -----------   get Destination() { return this.Origin.plus(this); }   // OriginCell   // ----------   get OriginCell() { return this.grid.at(this.Origin); }   // DestinationCell   // ---------------   get DestinationCell() { return this.grid.at(this.Destination); }   // Border   // ------   get Border() { return this.border; }   set Border(val) {       if (this.border == val) return;      this.border = val;      var destCell = this.DestinationCell;      if (destCell === null) return;      destCell.getDirection(this.mult(-1)).Border = val;   }   	// ----------------	// Method overrides	// ----------------   	// --------------	// Public methods	// --------------   }