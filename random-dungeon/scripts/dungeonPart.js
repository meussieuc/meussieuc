// -----------------------------------------------------------------------------// DungeonPart Class// -----------------------------------------------------------------------------class DungeonPart extends BaseClass {	// -----------	// constructor	// -----------   constructor(pos, grid) {      super();            if (this.constructor === DungeonPart)         throw new Error("Cannot instantiate abstract class DungeonPart");            if (pos === null)         throw new Error("Position can't be null");      		// ---------------		// Private members		// ---------------      this.pos  = pos.toVector();      this.grid = grid;      		// ---------------		// Private methods		// ---------------         }		// ---------	// Accessors	// ---------   // Pos   // ---   get Pos() { return this.pos; }      // SeedNr   // ------   get SeedNr() { throw new Error(`SeedNr gettor must be implemented by ${this.constructor}`); }   set SeedNr(val) { throw new Error(`SeedNr settor must be implemented by ${this.constructor}`); }   	// ----------------	// Method overrides	// ----------------   	// --------------	// Public methods	// --------------   }