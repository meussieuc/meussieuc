// -----------------------------------------------------------------------------// BaseClass Class// -----------------------------------------------------------------------------class Rectangle extends BaseClass {	// -----------	// constructor	// -----------   constructor(pos, width, height) {      super();      		// ---------------		// Private members		// ---------------      this.pos    = pos;      this.width  = width;      this.height = height;      		// ---------------		// Private methods		// ---------------         }		// ---------	// Accessors	// ---------   // Pos   // ---   get Pos() { return this.pos; }   set Pos(val) { this.pos = val; }      // Width   // -----   get Width() { return this.width; }   set Width(val) { this.width = val; }      // Height   // ------   get Height() { return this.height; }   set Height(val) { this.height = val; }      // Area   // ----   get Area() { return this.Width * this.Height; }      // MinCorner   // ---------   get MinCorner() { return this.Pos; }      // MaxCorner   // ---------   get MaxCorner() { return this.Pos.plus(new Vector(this.Width - 1, this.Height - 1)); }   	// ----------------	// Method overrides	// ----------------	// toString	// --------   	// --------------	// Public methods	// --------------   // containsCoords   // --------------   containsCoords(coords) {      var minCorner = this.MinCorner;      var maxCorner = this.MaxCorner;            return coords.X >= minCorner.X         && coords.X <= maxCorner.X         && coords.Y >= minCorner.Y         && coords.Y <= maxCorner.Y;   }}